import { RewardsPageContent } from "@/components/rewards/RewardsPageContent";

export const metadata = {
  title: "Rewards | OGEdge",
  description:
    "Earn cashback, unlock exclusive perks, and climb through six lifetime ranks. Your progress never resets.",
};

export default function RewardsPage() {
  return <RewardsPageContent />;
}
