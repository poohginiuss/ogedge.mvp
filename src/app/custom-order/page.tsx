import { CustomOrderPageContent } from "@/components/custom-order/CustomOrderPageContent";

export const metadata = {
  title: "Custom Order | OGEdge",
  description:
    "Need something done that isn't on the site? Place a custom order and we'll handle it for you.",
};

export default function CustomOrderPage() {
  return <CustomOrderPageContent />;
}
